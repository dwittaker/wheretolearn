<%= form_for([@portfolio,@opinion]) do |f| %>
  <% if @opinion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@opinion.errors.count, "error") %> prohibited this opinion from being saved:</h2>

      <ul>
      <% @opinion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :optitle %><br />
    <%= f.text_field(:optitle, :class => "input-xlarge") %>
  </div>
  <div class="field">
    <%= f.label :rating %><br />
    <%= f.text_field(:rating, :class => "input-xlarge") %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area(:comment, :class => "input-xlarge", :rows => 3 ) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
